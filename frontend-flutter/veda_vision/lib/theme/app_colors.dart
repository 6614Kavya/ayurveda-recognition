import 'package:flutter/material.dart';

class AppColors {
  static const bgStart = Color(0xFFF5F1E6);
  static const bgEnd = Color(0xFFE8F5E9);

  static const textPrimary = Color(0xFF2E4D34);
  static const titleColor = Color(0xFF355E3B);
  static const sectionTitle = Color(0xFF4A7C59);

  static const cardBg = Color(0xCCFFFFFF);
  static const cardShadow = Color(0x14000000);

  static const buttonBg = Color(0xFF6B8E23);
  static const buttonText = Colors.white;

  static const outputBg = Color(0xFFF0F7F2);
  static const inputBorder = Color(0xFFCCCCCC);

  static const loadingColor = Color(0xFF6B8E23);
  static const errorColor = Color(0xFFC0392B);
  static const amber = Color(0xFFD98C2B);

  // New — used by the capture-guidance banners
  static const infoBg = Color(0xFFEFF6FF);
  static const infoBorder = Color(0xFFB8D4F5);
  static const infoText = Color(0xFF2C5282);
}